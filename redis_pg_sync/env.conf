# Redis Configuration
REDIS_HOST=redis-cache
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=2
REDIS_POOL_SIZE=10
REDIS_MAXMEMORY=1gb
REDIS_MAXMEMORY_POLICY=volatile-ttl
REDIS_TIME_TO_LIVE=3600

# Redis Connection Management
REDIS_MAX_RETRIES=5
REDIS_BACKOFF_BASE=1.0
REDIS_BACKOFF_MAX=30.0
REDIS_HEALTH_CHECK_INTERVAL=60

# Redis DB Search Configuration
REDIS_DB_SEARCH_RANGE=0-15  # Comma-separated list of DB numbers or ranges
SOURCE_REDIS_DB=2

# Redis Stream Configuration
REDIS_STREAMS_ENABLED=true
REDIS_STREAMS=stream:pressure_bed:C2:7C:2C:F6:97:B7
STREAM_CONSUMER_GROUP=sync-group
STREAM_CONSUMER_NAME=sync-consumer
STREAM_BATCH_SIZE=100
STREAM_POLL_INTERVAL=1
STREAM_AUTO_DISCOVERY=false
STREAM_DISCOVERY_PATTERNS=*
STREAM_DISCOVERY_INTERVAL=10
STREAM_AUTO_CREATE_CONSUMER_GROUPS=true

# PostgreSQL Configuration
POSTGRES_HOST=EDN-4ed5fdd28e8e4103.elb.ca-central-1.amazonaws.com
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=redis_relay_user
POSTGRES_PASSWORD=MJ_bm_connector1
POSTGRES_SCHEMA=nexgen
POSTGRES_POOL_SIZE=10
POSTGRES_MAX_QUERIES=50000
POSTGRES_QUERY_TIMEOUT=30.0

# PostgreSQL Connection Management
PG_MAX_RETRIES=5
PG_BACKOFF_BASE=1.0
PG_BACKOFF_MAX=30.0
PG_HEALTH_CHECK_INTERVAL=60

# Table Configuration
STREAM_TABLE_NAME=redis_streams
KEY_VALUE_TABLE_NAME=redis_key_values

# Synchronization Configuration
SYNC_INTERVAL=5  # Redis to PostgreSQL sync interval in seconds
MONITOR_INTERVAL=5

# Key-Value Configuration
KEY_VALUE_AUTO_DISCOVERY=true
KEY_VALUE_PATTERNS=*

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Docker Configuration
REGISTRY=localhost:5000
TAG=latest ORT=6379
